/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["garne.com.ua"],
    },
    reactStrictMode: false,
    swcMinify: true,
};

module.exports = nextConfig;
